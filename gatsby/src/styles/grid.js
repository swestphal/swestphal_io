const grid = {
  // We just need to know these 3 values up front:
  wrapper: 100, //vw
  gutter: 20, //px
  noOfColumns: 4,

  // Number of gutters is columns minus 1:
  noOfGutters: 3,
};

export default grid;
